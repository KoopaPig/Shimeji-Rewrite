<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3
          https://maven.apache.org/xsd/assembly-1.1.3.xsd">

    <id>dist</id>

    <formats>
        <format>dir</format>
    </formats>

    <!-- This prevents double nesting -->
    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>
        <!-- Include compiled jar -->
        <fileSet>
            <directory>${project.build.directory}</directory>
            <includes>
                <include>${project.build.finalName}.jar</include>
            </includes>
            <outputDirectory>/</outputDirectory>
        </fileSet>

        <!-- Include project resources -->
        <fileSet>
            <directory>${project.basedir}</directory>
            <includes>
                <include>LICENSE.txt</include>
                <include>readme.txt</include>
                <include>originalreadme.txt</include>
                <include>changelog.txt</include>
                <include>kilkakon.txt</include>
                <include>img/**</include>
                <include>conf/**</include>
            </includes>
            <excludes>
                <exclude>conf/behaviors.xml</exclude>
                <exclude>conf/*Behavior.xml</exclude>
            </excludes>
            <outputDirectory>/</outputDirectory>
        </fileSet>
    </fileSets>

</assembly>